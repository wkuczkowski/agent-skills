interface:
  display_name: "Unslop"
  short_description: "Cut AI tells from writing"
  default_prompt: "Use $unslop to strip AI tells from the text I point at."
policy:
  allow_implicit_invocation: false
